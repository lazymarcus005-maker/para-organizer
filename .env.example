# ─── Core ───
PARA_PORT=8731
PARA_DB_PATH=/var/lib/para-organizer/data/para.db
PARA_SECRET_KEY=change-me-in-production

# ─── LLM (Ollama Cloud) ───
OLLAMA_API_KEY=<your-ollama-api-key>
OLLAMA_BASE_URL=https://ollama.com/v1
LLM_PRIMARY=deepseek-v4-flash
LLM_FALLBACK=gpt-oss:20b
LLM_TIMEOUT=60
LLM_MAX_RETRIES=2

# ─── Chat (conversational mode) ───
CHAT_MODEL=gpt-oss:20b
CHAT_HISTORY_MAX=20

# ─── Embeddings (hybrid RAG) ───
# EMBED_PROVIDER: "ollama_local" or "ollama_cloud" (native Ollama /api/embed), or
# anything else for an OpenAI-compatible /v1/embeddings endpoint.
EMBED_PROVIDER=ollama_local
EMBED_BASE_URL=http://localhost:11434
EMBED_MODEL=nomic-embed-text
EMBED_API_KEY=
EMBED_DIMENSIONS=768
RAG_HYBRID_ENABLED=true
RAG_HYBRID_RATIO=0.5
RAG_SEARCH_LIMIT=5

# ─── Telegram ───
TELEGRAM_BOT_TOKEN=<your-telegram-bot-token>
TELEGRAM_WEBHOOK_URL=https://para.mxlabs.cloud/webhook/telegram
TELEGRAM_ALLOWED_USERS=<comma-separated-user-ids>

# ─── Notification ───
NOTIFY_CHANNEL=telegram
NOTIFY_DEADLINE_DAYS=7,3,1
NOTIFY_DIGEST_DAY=mon
NOTIFY_DIGEST_TIME=08:00
NOTIFY_STALE_DAYS=14

# ─── Auto ───
AUTO_ARCHIVE_DAYS=30
RECLASSIFY_INTERVAL_HOURS=6
RECLASSIFY_CONFIDENCE_THRESHOLD=0.7

# ─── Database (PostgreSQL) ───
# PostgreSQL connection string for production deployments
PARA_DB_URL=postgresql+asyncpg://user:password@para-db:5432/para_organizer
# Connection pool settings
PARA_DB_POOL_SIZE=10
PARA_DB_MAX_OVERFLOW=20

# ─── Redis ───
# Redis connection string for caching and task queue
PARA_REDIS_URL=redis://para-redis:6379/0
# Cache TTL in seconds
PARA_REDIS_CACHE_TTL=60

# ─── Web ───
WEB_PUBLIC_URL=https://para.mxlabs.cloud